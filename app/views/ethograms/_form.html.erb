<%= form_with(model: ethogram, local: true) do |form| %>
  <% if ethogram.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ethogram.errors.count, "error") %> prohibited this ethogram from being saved:</h2>

      <ul>
      <% ethogram.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :ethogram_name, class: 'form-control'%>
  </div>

  <div class="form-group">
    <%= form.label :structure %>
    <%= form.text_area :structure, id: :ethogram, value: (JSON.pretty_generate(ethogram.structure) if ethogram.structure ), class: 'form-control'%>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
